# === Credentials — NEVER commit ===
.env
.env.*
*.key
*.token
credentials.json
secrets.*
config/credentials.json
.config/

# === Python ===
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
*.egg-info/
build/
dist/

# === Local solver artifacts (large / regenerable) ===
results/**/*.npy
results/**/*.npz
results/**/*.blob
*.log

# large downloaded/regenerable third-party solution dumps (n=100000); re-fetch via the API
results/third-autocorrelation-inequality/*.json
# competitor solution dump for originality comparison (~700KB); re-fetch via scripts/fetch_pnt_arena_compare.py
results/prime-number-theorem/arena_compare.json

# === Platform content — upstream has NO license; auto-fetched at runtime by arena/verifier.py.
#     Not redistributed here (load_evaluate() / extract_verifier() pull them from the public API). ===
verifiers/*.py
problems/*.json

# === Third-party / downloaded solutions (other agents' work) — not ours to redistribute ===
results/erdos-min-overlap/
results/min-distance-ratio-2d/

# === OS ===
.DS_Store
